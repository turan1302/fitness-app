import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  animated_container : { backgroundColor: "white", flex: 1 },
  list_header_style : {marginLeft : -10},
  content_container : { paddingHorizontal: 10 },
  column_wrapper : { justifyContent: "space-between" }
});

export default styles;
