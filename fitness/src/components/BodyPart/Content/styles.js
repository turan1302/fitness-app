import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  text_area : {paddingVertical : 20,paddingHorizontal : 10},
  text : {fontSize : 20,color : "black",fontWeight : "bold"}
});

export default styles;
